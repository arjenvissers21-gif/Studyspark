import {prisma} from "../../../lib/prisma";import {getCurrentUser} from "../../../lib/session";import Link from "next/link";
export default async function Page({params}:{params:Promise<{id:string}>}){const u=await getCurrentUser();const{id}=await params;if(!u)return <main className="container page"><h1>Log in om dit document te bekijken.</h1></main>;const d=await prisma.document.findFirst({where:{id,userId:u.id},include:{sections:{orderBy:{order:"asc"}},flashcards:true,quizzes:{include:{questions:true}}}});if(!d)return <main className="container page"><h1>Niet gevonden</h1></main>;return <main className="container page"><div className="row"><div><span className="pill">{d.sourceType}</span><h1>{d.title}</h1></div><div className="row"><Link className="btn soft" href={"/documents/"+id+"/study"}>Start studeren</Link><Link className="btn primary" href={"/api/documents/"+id+"/word"}>Word downloaden</Link></div></div><div className="card" style={{marginTop:20}}><h2>Samenvatting</h2><p>{d.summary}</p></div><div className="card" style={{marginTop:16}}><h2>Onderwerpen</h2>{d.sections.map(s=><section key={s.id}><h3>{s.title}</h3><p>{s.content}</p></section>)}</div><div className="card" style={{marginTop:16}}><h2>Flashcards</h2>{d.flashcards.map(c=><p key={c.id}><b>{c.question}</b><br/><span className="muted">{c.answer}</span></p>)}</div><div className="card" style={{marginTop:16}}><h2>Quiz</h2>{d.quizzes[0]?.questions.map(q=><p key={q.id}><b>{q.question}</b><br/><span className="muted">{q.answer}</span></p>)}</div></main>}
