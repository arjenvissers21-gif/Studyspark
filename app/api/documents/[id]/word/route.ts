import {NextResponse} from "next/server";import {prisma} from "../../../../../lib/prisma";import {makeDocx} from "../../../../../lib/word";import {getCurrentUser} from "../../../../../lib/session";
export const runtime="nodejs";export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){const u=await getCurrentUser();if(!u)return NextResponse.json({error:"Log eerst in."},{status:401});const{id}=await params;const d=await prisma.document.findFirst({where:{id,userId:u.id},include:{sections:{orderBy:{order:"asc"}},flashcards:true}});if(!d)return NextResponse.json({error:"Niet gevonden."},{status:404});const b=await makeDocx({title:d.title,summary:d.summary||"",sections:d.sections,flashcards:d.flashcards});return new NextResponse(b as BodyInit,{headers:{"Content-Type":"application/vnd.openxmlformats-officedocument.wordprocessingml.document","Content-Disposition:`attachment; filename="${encodeURIComponent(d.title)}.docx"`}})}
